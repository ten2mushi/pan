Amplitude from each recorded audio signal is sampled at 60 frames per second and translated into data points distributed in 3D space.

The spatial distribution is shaped by audio driven oscillatory patterns closely mirroring the various features, amplitude, and temporal characteristics found in the spectrogram.

This continuous stream forms ever changing generative structures providing a three-dimensional, data-driven visualization of sound.


Data for every point:

COLOR/FREQUENCY  
Most active frequency band at Emission Time

SIGNAL AMPLITUDE  
scaled from 0 to 1

LIFETIME  
in seconds, animated,  
starting from 0 at Emission Time

EMISSION TIME  
in seconds, with Time=0  
at the beginning of every animation

The diagram includes a central red square (with a white dot) surrounded by example numerical values (0.0000, 0.00, 0.0, 0.00) that illustrate the data attributes for an individual point. A horizontal color bar at the top ranges from 2K Hz (blue) to 8K Hz (cyan/white), corresponding to the frequency scale.

----

**This does not appear to be a single, standardized analytical technique** with a widely recognized proper name (such as “PCA,” “t-SNE,” or a specific MIR algorithm). Instead, the screenshots document a **custom generative 3D audio visualization pipeline** that maps real-time or recorded audio features into an evolving three-dimensional particle/point-cloud system.

### Core Components of the Pipeline

The system integrates several well-established methods into a cohesive, real-time (or frame-accurate) generative visualization:

1. **Spectral Feature Extraction**  
   - Audio is analyzed via short-time Fourier transform (STFT) or equivalent spectrogram computation.  
   - Per-frame (here sampled at 60 fps) features extracted include:  
     - Overall amplitude (scaled 0–1)  
     - Dominant / most active frequency band (mapped to color via the visible 2 kHz–8 kHz scale)  
     - Temporal evolution of these features

2. **Particle / Point-Cloud Generation & Attribute Mapping**  
   - Each visual element is a data point (particle) with four explicit attributes, as labeled in the second screenshot:  
     - **Emission Time** — birth timestamp (Time = 0 at start of animation)  
     - **Lifetime** — duration in seconds; the point is animated and fades/disappears after its lifetime  
     - **Signal Amplitude** — scaled 0–1, typically controlling size, brightness, or opacity  
     - **Color / Frequency** — most active frequency band at the moment of emission (mapped to the visible color gradient)

3. **Spatial Distribution via Audio-Driven Oscillatory Patterns**  
   - Particle positions in 3D space are not random; they are shaped by oscillatory (sinusoidal or more complex periodic) functions whose parameters are modulated by the extracted audio features.  
   - This creates structures that “closely mirror the various features, amplitude, and temporal characteristics found in the spectrogram,” producing the organic, ever-changing generative forms shown.

4. **Rendering & Animation**  
   - Continuous emission of new points creates a streaming point cloud / network (with visible connecting lines in the visualization).  
   - Points have finite lifetimes, producing the “continuous stream [that] forms ever changing generative structures.”  
   - The entire system runs at 60 fps to maintain smooth, temporally accurate correspondence with the audio.

### Common Names & Contexts for This Approach

In practice, practitioners and researchers refer to systems with these characteristics using descriptive terms such as:

- **Audio-reactive 3D particle system** (most common in creative coding / VJ communities)
- **Spectral / spectrogram-driven particle visualization**
- **Generative 3D point-cloud visualization of sound**
- **Data-driven / feature-mapped 3D audio visualization pipeline**
- **Real-time audio feature sonification / visualization** (when the goal is analytical insight rather than purely aesthetic)

This style of pipeline is frequently implemented in node-based real-time environments such as **TouchDesigner**, **Houdini**, or custom code in Unity/Unreal Engine with audio analysis plugins, or in creative-coding frameworks (Processing, openFrameworks, p5.js + WebGL, etc.).

### Summary

The technique shown is a **bespoke audio-to-3D generative visualization system** that uses spectral analysis to drive a lifetime-managed particle/point system in three-dimensional space. While it draws on standard building blocks (STFT/spectrogram analysis + particle systems + attribute mapping), the specific combination, the explicit “Data for every point” schema, and the emphasis on oscillatory spatial patterning appear to be a custom artistic/technical implementation rather than a named, off-the-shelf analytical method.

